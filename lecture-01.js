//while : when we know the condition but don't know how much iterations it's need to complete.
var fileSize = 1024;
var currentFileDownload = 0;

while(currentFileDownload <= fileSize){
    console.log(`Keep Downloading the file : `);
    currentFileDownload =+ 40;
}

